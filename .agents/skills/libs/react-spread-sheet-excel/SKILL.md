Library: react-spread-sheet-excel

When to use:

- Used for spreadsheet-like components or Excel interactions in the UI.

Steps:

1. Wrap the library in a feature-specific component under `src/features/` to isolate behavior.

Minimal snippet:

- See project usage where spreadsheet components are rendered.

## Update notes:
