Library: lucide-react (icons)

When to use:

- Use `lucide-react` for vector icons; create small icon wrapper components for consistent sizing.

Steps:

1. Import icons where needed and wrap them in a small presentational component if reused.

Minimal snippet:

- `import { IconName } from 'lucide-react'`

## Update notes:
