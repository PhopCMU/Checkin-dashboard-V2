Library: crypto-js

When to use:

- Use for lightweight client-side cryptographic helpers (matching existing repo usage).

Steps:

1. Wrap crypto-js usage behind small helpers in `src/utils/authService.ts` or similar.

Minimal snippet:

- `import CryptoJS from 'crypto-js'`

## Update notes:
