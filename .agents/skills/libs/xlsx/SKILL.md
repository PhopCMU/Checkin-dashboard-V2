Library: xlsx (SheetJS)

When to use:

- When working with legacy workbook exports or direct XLSX manipulation.

Steps:

1. Prefer the repo helpers in `src/utils/` for user-facing exports.

Minimal snippet:

- `import * as XLSX from 'xlsx'`

## Update notes:
