Library: react-datepicker

When to use:

- Use for date-picking components in forms; prefer local wrappers for consistent styling.

Steps:

1. Wrap `react-datepicker` in a local component under `src/components/` to centralize props and styles.

Minimal snippet:

- `import DatePicker from 'react-datepicker'`

## Update notes:
