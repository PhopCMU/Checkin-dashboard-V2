Library: date-fns

When to use:

- Use for date parsing, formatting, and date math.

Steps:

1. Prefer immutable operations and format strings in utilities under `src/utils/attendance.ts`.

Minimal snippet:

- `import { format } from 'date-fns'`

## Update notes:
