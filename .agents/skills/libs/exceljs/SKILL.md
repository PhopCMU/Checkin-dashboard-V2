Library: exceljs

When to use:

- Use for generating Excel files server-side or in scripts; for client exports prefer the repo's `exportXlsxForUser` util.

Steps:

1. Prefer the existing helper under `src/utils/exportXlsxForUser.ts` for consistent exports.

Minimal snippet:

- See `src/utils/exportXlsxForUser.ts` for project-specific usage.

## Update notes:
